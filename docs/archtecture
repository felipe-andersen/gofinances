repositório é ambiente provisório para armazenamento de dados antes de ir para um lugar determinado.


Funcionalidades como casos de uso.

use case

adapters


a organização, métodos de acesso e opções de processamento

Estruturas lineares
estrutura de associação


circuitos digitais , dois estados (on ou off, verdadeiro ou falso, 0 ou 1)

bytes (grupos de 8 bits), 
words (dois bytes ou 16 bits) 
long words (quatro bytes ou 32 bits)


padrão de codificação

Tipos primitivos, dados básicos  escalares através da declaração de variáveis 


núymeros interiros +0 === -0  resulta em true.
números com pontos flutuantes


representar muitos valores booleanos dentro de um único número usando bit masking


string é o número de elementos nela.

caracter de escape 


 operadores, estruturas de controle e declarações.

 sistema em tempo de compilação das classes

 sistema em tempo de execução

 sisteme em tempo de compilação


  modelo de objeto baseado em protótipo em vez do modelo, mais comum, de objeto baseado em classes. 


   linguagem de programação baseada em classes,

   variáveis e identificadores

   classes, objetos

   copiar quando é para ser copiado, entender os padrões para improvisar e entender a lógica.


aplicativo precisa ser publicado
a chave dados de autenticação do app faz com que o provedor libere o acesso/api para o aplicativo


tratar erros é essêncial


hash


endpoit 


classe é abstrato instancia e parte de uma classe, é a instanciação de uma classe, ou seja, um dos seus membros.


Qualquer objeto pode especificar suas próprias propriedades, quando você o cria ou em tempo de execução. 


   